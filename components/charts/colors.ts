// Single-hue palette shared by all analytics charts (each chart is one series,
// so no categorical/multi-hue assignment is needed here).
export const CHART_COLORS = {
  series: "#2a78d6",
  grid: "#e1e0d9",
  axis: "#c3c2b7",
  mutedText: "#898781",
};
